#include <cstdio>
#include <cstring>
using namespace std;
#define FOR(val, start, end) for(int val=(start); val<(end); val++)

int main() {
    char str[101]; scanf("%s", str);
    printf("%d\n", strlen(str));
    return 0;
}